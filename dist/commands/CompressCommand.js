import o,{existsSync as e,readFileSync as t}from"node:fs";import{normalize as r}from"node:path";import{fatal as s}from"@triforce-heroes/triforce-core/Console";import{compress as i}from"../Compress.js";export function CompressCommand(m,n,f){e(m)||s(`File not found: ${m}`);let p=r(n??`${m}.compressed`),c=t(m);process.stdout.write(`Compressing ${r(m)} to ${p}... `);let l=Date.now(),d=i(c,f.level);process.stdout.write(`OK (${((Date.now()-l)/1e3).toFixed(2)}s)
`),o.writeFileSync(p,Buffer.concat([d,Buffer.alloc(Math.max(0,f.width-d.length))]))}